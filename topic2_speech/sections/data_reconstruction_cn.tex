\section{数据集重建与标注规则}

\subsection{筛选规则}

本文使用的子集由 \path{src/prepare_mswc_initial_gbdz_subset.py} 生成，规则很简单：
\begin{quote}
保留一个单词，当且仅当它的首字母属于 \texttt{g/b/d/z}，并且 CMUdict / ARPABET 中首个非元音音素与该字母严格对应为 \texttt{G/B/D/Z}。
\end{quote}

这条规则的作用，是把“拼写里有目标字母”收紧成“这个目标辅音确实出现在起始位置”。这样一来，数据不再混入发音位置不对的样本，也不再把字母出现位置和实际发音边界混在一起。

先检查原始候选池后可以看到，100 个词/类在严格规则下做不到：只要求每个词至少有 1 条 train、1 条 dev、1 条 test 时，/g/、/b/、/d/ 分别有 957、2073、1932 个候选词，而 /z/ 只有 75 个。因此最终按 /z/ 的上限取每类 75 个词。为了避免只增加单个词的音频数量，脚本使用 \texttt{balanced-cap} 采样：每个词每个 split 至少保留 1 条音频，每词最多取 40/5/5 条，然后把四个标签在 train/dev/test 上截到同样的样本数。

\subsection{数据规模}

\begin{table}[H]
    \centering
    \caption{精确首浊辅音子集的规模统计}
    \label{tab:dataset}
    \begin{tabular}{lcccc}
        \toprule
        标签 & 词数 & train & dev & test \\
        \midrule
        /g/ & 75 & 1311 & 194 & 194 \\
        /b/ & 75 & 1311 & 194 & 194 \\
        /d/ & 75 & 1311 & 194 & 194 \\
        /z/ & 75 & 1311 & 194 & 194 \\
        \midrule
        合计 & 300 & 5244 & 776 & 776 \\
        \bottomrule
    \end{tabular}
\end{table}

这个子集在标签词数和每个 split 的标签样本数上都是平衡的，测试集每类 194 条，总计 776 条。相比 60 词版本，它牺牲了固定每词样本数，但显著增加了词表覆盖，更接近“跨词判断首浊辅音”而不是记住少数高频词。
